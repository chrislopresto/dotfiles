[user]
  name = Chris LoPresto
  email = chris.lopresto@betterment.com

[credential]
  helper = /opt/boxen/bin/boxen-git-credential

[color]
  ui = true

[core]
	editor = subl -n -w
  excludesfile = ~/.gitignore

[mergetool]
  keepBackup = true

[push]
  default = simple

[filter "media"]
  required = true
	clean = git media clean %f
	smudge = git media smudge %f
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
[github]
	user = chrislopresto
[ghi]
	token = !security find-internet-password -a chrislopresto -s github.com -l 'ghi token' -w

[include]
  path = .gitthings
